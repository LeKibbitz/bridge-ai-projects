describe('Basic Tests', () => {
  test('should pass', () => {
    expect(true).toBe(true);
  });

  test('should handle basic operations', () => {
    expect(1 + 1).toBe(2);
    expect(2 * 2).toBe(4);
  });
});
